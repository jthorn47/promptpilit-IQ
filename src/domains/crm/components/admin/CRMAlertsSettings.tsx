// TEMPORARILY DISABLED - Component disabled due to build errors
// Will be re-enabled after schema fixes are complete

import React from 'react';

export function CRMAlertsSettings() {
  return (
    <div className="p-4">
      <h3 className="text-lg font-semibold mb-4">CRM Alerts Settings</h3>
      <p className="text-muted-foreground">
        This component is temporarily disabled while schema fixes are being applied.
        It will be re-enabled once all TypeScript errors are resolved.
      </p>
    </div>
  );
}